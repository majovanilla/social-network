<header>
    <nav id='logo' class="navbar navbar-expand-lg navbar-light bg-light">
        <p class="navbar-brand">SocialBook</p> 
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="collapse navbar-collapse list-style-none" id="navbarSupportedContent">
                <% if user_signed_in? %>
                <li class="nav-item active"><%= link_to "Home", root_path, class:"nav-link" %></li>
                <li class="nav-item dropdown">
                    <%= link_to "Profile", root_path, id:"navbarDropdown", class:"nav-link dropdown-toggle" %>
                  
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <a class="dropdown-item" href="#"><%= current_user.username %></a>
                        <a class="dropdown-item" href="#">Settings</a>
                        <div class="dropdown-divider"></div>
                <li class="dropdown-item"><%= link_to "Log out", destroy_user_session_path, method: 'delete' %></li>
        </div>
        </li>
        <% else %>
        <li class="nav-item active"><%= link_to "Sign Up", signup_path, class:"nav-link" %></li>
        <li class="nav-item active"><%= link_to "Log in", login_path, class:"nav-link" %></li>
        <% end %>
        </ul>
        </div>

    </nav>
</header>