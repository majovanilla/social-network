<h2 class="text-center my-5">Notifications</h2>
<ul class="users-list">
    <% @friend_requests.each do |user | %>
    <li class='user-info col-12'>
        <div class="row col-9 mx-auto my-5 d-flex justify-content-center">
            <div class='col-6 row'>
                <div class="col-5"> <%= image_tag user.gravatar_url %> </div>
                <p class="col-7"><%= link_to user.username, user_path(user.id) %></p>
            </div>
            <div>
                <%= button_to 'Accept Friendship', friendships_path(friendship: { user_id: current_user, friend_id: user }), method: :post, class:"btn btn-primary" %>
            </div>
        </div>
    </li>
    <% end %>
</ul>